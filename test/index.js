'use strict'

//----------------------------------------------------------
// modules
//----------------------------------------------------------
// npm
const chai = require('chai').assert

// local
const nomu = require('../')

//----------------------------------------------------------
// tests
//----------------------------------------------------------
nomu.src('../node_modules/*/*.js')
  .then(map => map.forEach((_, key) => console.log(key)))
